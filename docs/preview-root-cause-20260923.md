# 32 路预览开销归因（两轮已完成）

> 阅读顺序：3。主要目的：汇总预览等待路径及成功、失败的优化实验，区分已证实与待定位机制。 [返回阅读导航](pcie-reading-guide.md)。

代码确认每路推理线程同步执行 Detect → Thumbnail::render（VPP → D2H → 画框）→ wall.submit，再进入下一轮取帧。这证明存在串行依赖，不证明 VPP 硬件饱和或 PCIe 总线满载。

独立 build-ablation 二进制新增 --preview-stage display|vpp|readback，默认 display 保持正常路径；观察解码模式与诊断阶段互斥。原 build/hdmi_wall.pcie 未替换。六项现有原生测试通过；诊断模式的硬件行为将由本轮数据核对。

测试目录 data/results/preview-stages32-20260923，32 路原1080p24长素材，device0 PCIe2x1，筛选、64KiB合并、reuse、250ms门槛。每档30秒预热60秒正式，正反两轮：

- original：原二进制完整预览，检验新编译版本基线差异。
- none：诊断二进制，preview-fps 0。
- vpp：预览上限10，只做小图VPP，不D2H、不画框、不提交画面。
- readback：VPP＋D2H，不画框、不提交画面。
- display：完整预览。

显示模块和播放器在各档均保留，因此 readback→display 差异包括画框和提交新画面的增量，不是整个显示子系统开/关对照。每次预览完成后才重新计时，实际尝试频率会随成本变化；需报告实际预览尝试数，不能把相邻档差值简单当成可相加的固定单阶段成本。

诊断模式中的 previews_submitted 字段沿用旧名，实际表示预览尝试完成次数；只有 display 模式确实提交图像。VPP-only 的 preview_readback_bytes_measured 为0；耗时为SDK调用墙钟，包含等待。两种诊断截断模式不会显示动态小图，这是预期行为。

下一步在独立运行中抓取驱动调用及线程等待；不要在本对照中途附加 strace，以免改变单档负载。硬件未支持的计数器不能凭空补齐。链路强制降速/插槽切换尚未执行。

## 两轮结果

| 模式 | INF FPS | TPU % | 预览尝试/秒（全卡） | 小图 MB/s | 最长空窗 s |
|---|---:|---:|---:|---:|---:|
| original | 210.48 | 76.32 | 153.08 | 16.93 | 2.479 |
| none | 264.11 | 96.20 | 0.00 | 0.00 | 0.248 |
| vpp | 221.71 | 80.71 | 153.08 | 0.00 | 0.278 |
| readback | 213.08 | 78.03 | 152.87 | 16.91 | 2.482 |
| display | 210.70 | 76.67 | 152.66 | 16.88 | 1.948 |

新旧完整显示基线约210.70/210.48 FPS，差约0.1%，未观察到明显构建偏移。无预览264.11 FPS，VPP-only221.71，加入D2H213.08，完整显示210.70。VPP-only在两轮均出现明显下降，且缩略图回传字节为0、最长空窗低于0.28秒。因此“小图像素经PCIe回传”和“严重帧过期中断”不是本轮VPP-only下降的必要条件；同步小图生成路径本身足以造成下降。

VPP-only调用均值两轮33.15/30.69ms；包含SDK、驱动、排队、硬件执行等，不是裸VPP时间。代码表明它在每路推理循环内同步执行，下一帧推理必须等它返回。此为已确认的应用级串行依赖。是否还包含VPP硬件竞争、驱动锁、设备内存访问或主机调度，尚未分解，不应直接归因于PCIe线速或VPP饱和。

无预览到VPP-only的下降约42.40 FPS，完整预览总下降约53.41 FPS；不能将比值当成硬件成本百分比，因为实际预览频率及资源竞争随模式变化。显示链路在所有模式仍运行，末两档差值不是关闭整个显示系统的收益。

455个普通文件已逐一SHA256校验一致；10个FIFO不复制。原始数据和逐档计时见 data/results/preview-stages32-20260923/comparison.json；预览尝试率见 ablation-analysis.json。全部10档正常完成。

## 驱动路径线索（尚未测量各段等待）

板端 /opt/sophon/driver-0.5.1/vpp/bm1686_vpp.c 的 vpp_handle_setup 先 down_interruptible 获取 vpp_core_sem，再准备命令与启动硬件，最后 wait_event_timeout 等待完成中断后释放核。代码明确存在获取核与等待完成两处阻塞，但仅凭附带源码不能证明当前已加载模块逐字一致，也不能量化这两个阶段在本轮的占比。源码已保存在本地 .tools/vpp-driver-source。

本次读取 available_filter_functions 未发现 vpp_handle_setup 可直接作为该名称的函数追踪点，只发现相关中断函数。未改全局 tracing 设置，未修改驱动。后续需要核对实际模块符号与版本，再选用可区分获取核等待、命令提交、完成等待的跟踪方式。此项仍未完成，不应称为已证实驱动锁瓶颈。

## 实际驱动调用追踪：VPP 核准入等待已确认

2026-09-23 独立短测，none/vpp 各20秒预热20秒正式，窗口中单独启用约4秒 function_graph，根入口 bm1686_trigger_vpp，sleep-time=1（包含睡眠等待）。没有修改驱动。内核未启用KPROBES，故使用全局函数图；原追踪为nop且过滤器为空，结束后恢复原全部修改项及7KiB缓冲。实际加载模块srcversion与磁盘模块一致：9C14781EC1530C39E702F94；模块SHA256为 fcf71e84f0797371ec85e9d6989c71d7a58162134e3ffa7dd8f26687205a40d9。

第一轮7KiB追踪缓冲覆盖严重，仅作为探路、不纳入统计；第二轮每CPU 8192KiB，所有CPU overrun/commit overrun/dropped events均为0。按PID栈匹配完整函数调用，排除窗口边缘未闭合调用。

| 模式 | 完整调用数 | 入口平均ms | down_interruptible平均ms | 命令S2D平均ms | 提交后schedule_timeout平均ms |
|---|---:|---:|---:|---:|---:|
| 无缩略图 | 4211 | 41.455 | 39.643 | 0.682 | 1.028 |
| 仅生成缩略图 | 4566 | 41.407 | 39.742 | 0.575 | 0.988 |

函数图直接证明主要阻塞位于 bm1686_trigger_vpp 内部的 down_interruptible，而不是入口之后的数据复制或完成等待。附带驱动源码与模块反汇编支持该位置对应 vpp_core_sem 获取；源码管理两个VPP核，获得核后准备命令、启动、等完成，再释放。推理预处理与缩略图均调用VPP接口，因此多路预处理和额外缩略图请求共享这一准入资源。

这是“具体驱动资源等待＋程序串行依赖”的证据链：32路请求等待VPP核；同步预览增加这条共享路径的请求，并让该路下一次推理必须等预览返回。此前不带追踪的拆分试验已证明只添加VPP就使264.11→221.71 FPS，而无需小图D2H即可复现。

限制：追踪影响调度和耗时，41ms与39ms只用于本次追踪窗口，不能替代无追踪SDK均值或作为生产性能承诺；本次入口包括预处理等所有VPP请求，未逐请求标注业务来源。完成等待包含中断和调度，不是纯硬件执行时间。准入等待证明争用，但不能直接断言两个硬件核的计算单元100%繁忙；占核期间还包含命令传送及完成通知。不能据此宣布PCIe链路完全无影响，或承诺改异步就一定恢复满载。

原始证据：data/results/vpp-driver-graph2-20260923，91个普通文件SHA256逐一一致，分析结果 *.trace.analysis.json。分析脚本 .tools/analyze_vpp_graph.py，复现脚本 .tools/trace_vpp_graph2.py，模块反汇编 .tools/bm1686-trigger-disassembly.txt。下一步优化验证应减少重复VPP请求、批处理或控制VPP并发，另评估异步预览能解除线程串行依赖到何种程度；仅异步化不减少共享VPP工作量。

## 异步预览验证（已完成，不能作为稳定优化采用）

新增实验参数 --preview-mode sync|async，默认sync，独立 build-async；保留原build和build-ablation。异步每路最多一个pending＋一个active的Frame/boxes配对，引用计数保留解码表面；预览线程退出及释放表面在解码器销毁前完成。禁止与catchup、observe-decode、非display阶段及强制BGR路径组合，当前实验只验证直接YUV路径。

data/results/async-preview32-20260923：相同新二进制sync/async各两轮、正反顺序，各30秒预热60秒正式。32路原24FPS素材、检测结果回传开启、10FPS预览上限。仅改变线程分工，VPP总工作量不保证减少。六项既有原生测试通过；并发正确性与退出仍需本轮运行验证。

异步预览指标由预览线程独立统计，按完成落入正式窗口计数；同步按检测完成记账，两者边界不同。实际预览FPS、小图字节、最差分析空窗与总INF须同时报告。不能把异步预览耗时加回同步分析时间线。旧config中的max_outstanding_per_stream估计尚未含异步新增最多两张引用，本实验以此处生命周期说明为准。


两轮正式结果：

| 模式 | 平均总INF | 平均TPU | 平均实际预览FPS/路 | 最差单路INF | 最长分析空窗 |
|---|---:|---:|---:|---:|---:|
| 同步 | 213.80 | 78.04% | 4.78 | 6.43 | 1.389秒 |
| 异步 | 250.76 | 92.37% | 5.77 | 0.68 | 47.362秒 |

异步吞吐提高约17.3%，同时实际预览增加，证明应用线程解耦能改善总吞吐，但严重损害部分通道连续性：两轮最大空窗分别39.309/47.362秒。不能宣布优化成功，不改同步默认、不替换正式二进制。零结果通道均为0仍掩盖了长期无结果问题。

逐秒状态各档均有32路has_image，发布画面计数增长；这是软件提交画面的证据，并非人工目视HDMI或录屏验收。异步预览独立完成口径不同于同步检测归属口径。185个原始普通文件SHA256校验一致。下一步应限制预览资源占用并检查解码表面持有与源时钟落后；仅凭此次长停顿不能指定底层原因。

## 预览频率预算验证（已完成）

新目录 data/results/preview-budget32-20260923，入口run_preview_budget_ab.py。同一build-async，sync10基线及sync3/async3/sync5/async5，正反两轮，每档30秒预热60秒正式。固定32路、原1080p24、gate64reuse及结果回传开启。既比较降频的折中，也在同频下比较同步/异步，不能混称异步收益。关键判据为每路连续性和实际预览速率，吞吐不能掩盖长停顿。默认仍为同步，尚无本轮结论。


| 模式 | 总INF均值 | TPU均值% | 实际预览FPS/路 | 最差单路INF | 最长空窗s | 已完成帧源年龄最大ms |
|---|---:|---:|---:|---:|---:|---:|
| sync10 | 213.82 | 78.39 | 4.77 | 6.47 | 1.883 | 462.64 |
| sync3 | 246.43 | 90.05 | 2.32 | 7.17 | 3.430 | 421.46 |
| async3 | 266.96 | 97.90 | 2.66 | 8.30 | 0.218 | 381.50 |
| sync5 | 232.65 | 84.35 | 3.54 | 6.73 | 4.403 | 433.60 |
| async5 | 263.51 | 97.14 | 4.05 | 7.47 | 5.695 | 436.05 |

异步3 FPS上限是本轮唯一同时保持高吞吐和短分析空窗的候选：相对同频sync3，推理提升约8.3%，实际预览由2.32至2.66 FPS/路，最低单路8.30 FPS，最长空窗0.218秒。相对sync10，预览流畅度降低，这是质量折中，不能称免费收益。async5虽TPU97.1%仍有5.695秒空窗，不选作稳定候选。

各档均无零结果通道，正常退出。455个普通文件SHA256一致，10个FIFO不复制；逐秒状态用于核对图像提交，不代替目视HDMI。源年龄最大值只覆盖完成帧，不覆盖无结果空窗。两轮各60秒仍是短测，async3未通过长稳验收，默认sync保持不变。后续应对async3进行不触发EOF的30分钟测试，并核查逐路预览刷新间隔和分析连续性。


## PCIe3 x1跨卡复测

同程序sync10下，PCIe2为214.21FPS/78.03%，PCIe3 x1为260.94FPS/95.43%；VPP缩略图调用均值32.78/16.84ms。说明平台差异需要继续解释，不能称为纯软件且接口无关。完整证据及限制见 [跨卡报告](pcie-link-card-comparison-20260923.md)。


## 同口径跨卡VPP驱动追踪：未解释两卡性能差异

独立使用同一build-async、sync10、32路原素材，device0后device2，各20秒预热20秒测量，约4秒function_graph窗口，max_depth=2、sleep-time开启、每CPU8192KiB。非前述无追踪正式对照，不能混算吞吐。

| 平均耗时 | PCIe2 x1（4517次完整调用） | PCIe3 x1（5070次完整调用） |
|---|---:|---:|
| bm1686_trigger_vpp整体 | 40.688ms | 41.079ms |
| down_interruptible准入 | 39.000ms | 39.583ms |
| bmdev_memcpy_s2d_internal命令传送 | 0.596ms | 0.423ms |
| schedule_timeout完成等待 | 0.995ms | 0.976ms |

结果不支持“PCIe3的VPP准入排队必然更短”的解释：本次追踪两卡均大量等待且均值接近。命令传送调用PCIe3约少0.174ms，但不能将其直接解释为无追踪预览SDK调用约16ms差距。请求数、请求来源、队列深度不同，且函数图会改变调度；两个均值相近不代表原始性能相同。

因此已证实的范围应保持为：VPP准入竞争和应用同步依赖存在；无追踪跨卡性能差异可复现；本轮驱动追踪未解释跨卡差异。不能把资源等待直接定性为PCIe2专属问题或VPP硬件计算饱和。下一步应使用低扰动固定图像VPP探针，统一操作规格和并发数（1/2/4/8/16/32），分别比较无排队服务时间与并发扩展，再按需要考虑同卡链路降速验证。

两个追踪窗口所有CPU均无overrun/commit overrun/dropped events。91个普通文件SHA256一致，证据目录data/results/vpp-link-graph-20260923。trace设置恢复原nop、7KiB和原选项，无驱动修改。


## 固定图像VPP并发探针（两轮完成）

同一探针二进制SHA256 df8d926765fe4eb23e88488323563033d0dbe877312135de23f0751ed378514a；驻留NV12 1920x1080灰图转256x144 BGR，BT709、linear，各线程独立handle与内存；计时内没有逐次像素上传/回传、解码、推理、显示，也未启用函数图。每档3秒预热10秒计数，设备0→2→2→0，并发顺序正反，24阶段均通过输出一致性及灰值校验。

以下调用吞吐和均值取两轮算术平均；最差线程速度取两轮最差。仅完整落入计时窗口的调用计数，SDK墙钟含排队。

| 并发 | PCIe2调用/s | PCIe3调用/s | PCIe2平均ms | PCIe3平均ms | PCIe2最差线程调用/s | PCIe3最差线程调用/s |
|---|---:|---:|---:|---:|---:|---:|
| 1 | 1328.60 | 1496.40 | 0.752 | 0.667 | 1326.70 | 1496.30 |
| 2 | 2733.25 | 2909.05 | 0.731 | 0.687 | 1360.60 | 1454.10 |
| 4 | 2394.80 | 2747.85 | 1.669 | 1.455 | 597.70 | 686.20 |
| 8 | 2384.15 | 2741.85 | 3.353 | 2.916 | 297.50 | 342.50 |
| 16 | 2395.05 | 2736.30 | 6.674 | 5.843 | 149.20 | 170.80 |
| 32 | 2417.65 | 2738.15 | 13.218 | 11.673 | 75.20 | 85.40 |

两卡均在2线程附近达到此探针的最高吞吐，4线程起吞吐下降并进入平台，继续增加并发主要增加调用等待；并非线程越多服务能力越大。PCIe2单线程0.752ms，32线程13.218ms；PCIe3为0.667/11.673ms。该曲线支持有限服务资源与排队机制，与驱动准入等待证据一致，但不能由此证明硬件计算单元100%占用。

PCIe3单线程吞吐约高12.6%，32线程约高13.3%，差异在无视频墙/无逐次像素传输下仍存在，不能全归为预览线程调度。控制提交、驱动、设备内存/硬件状态以及卡与链路都仍是可能因素；跨卡不能等同同卡链路因果。探针没有复制真实解码表面的布局、物理地址及混合预处理负载，不能将2400次/s换算成32路真实业务能力，或用13ms直接替代业务VPP调用时间。

两线程到四线程的吞吐回落值得用固定操作的较低并发限流复验；全视频程序的总并发限流可能同时饿住TPU，不应未经测试设成2。下一步应针对VPP调用边界限制并发或批处理，并使用真实解码表面匹配探针，检查吞吐及最差分析空窗。

原始目录data/results/vpp-fixed32-20260923，77个普通文件SHA256一致；analysis.json保留逐阶段数据，verification.json保留校验。探针源码tools/diagnostics/vpp_fixed_probe.cpp。此测试只验证缩放格式转换输出一致性，不是检测精度验证。

## 真实视频VPP准入限流（已完成，吞吐下降，不采用）

新独立build-vpp-limit，环境变量HDMI_VPP_LIMIT默认0不限制，实验2或4。单进程共享FIFO票号准入器，同时覆盖预处理VPP段及缩略图VPP段，返回后释放，不覆盖TPU推理及结果/图片D2H。预处理准入段包含少量padding参数准备；不是纯内核入口计时。等待移到用户态不必然降低总等待，必须看实测。

本轮为device0 PCIe2x1、32路原24FPS、同步10FPS预览上限、检测结果回传开启，limit0/2/4正反各一轮、30warmup60正式，新目录data/results/vpp-limit32-20260923。不降低画面规格或频率。同一新二进制比较，原程序不替换。16线程争用测试覆盖limit1/2/4和重复release，均完成且未超过限制；六项既有原生测试通过。新增测试已纳入CMake供后续复跑。

统计实际推理与预览速度、最慢通道和分析空窗，若仅让SDK计时变短却降低吞吐或加剧停顿则不采用。此实现只覆盖本进程两个显式调用点，不限制解码器/SDK内部其他VPP任务，因此不能当作整卡总并发上限。


| 准入并发 | 总INF均值 | TPU均值% | 实际预览FPS/路 | 最差单路INF | 最长空窗s | 预处理ms | 小图VPP段ms |
|---|---:|---:|---:|---:|---:|---:|---:|
| limit0 | 212.12 | 77.97 | 4.76 | 6.45 | 1.479 | 70.60 | 33.79 |
| limit2 | 177.04 | 62.85 | 4.38 | 5.35 | 0.292 | 88.64 | 80.14 |
| limit4 | 186.75 | 67.21 | 4.42 | 5.72 | 0.289 | 88.21 | 75.22 |

limit2/4相对不限流总吞吐分别下降约16.5%/12.0%，TPU和实际预览速率均降低，不作为性能优化采用，默认仍0。虽然本轮最大分析空窗从1.479秒降至约0.29秒，但短测不能证明长期连续性修复，也不能忽略吞吐损失。

小图VPP段含新增用户态准入等待，不能把80ms称为硬件执行时间。当前准入器只覆盖本进程预处理与缩略图显式VPP段，无法约束解码/SDK内部调用；固定探针2线程吞吐峰值不能直接迁移为完整业务的最佳并发。这个负结果反驳“只需加2个并发的用户态限流就能解决”的方案，不反驳已观测到VPP资源排队。

六阶段均正常退出、零结果通道均0，原始275个普通文件SHA256一致，6个FIFO不复制。命令确认仅准入环境变量及输出路径改变；快照检查记录于status-check.json。原始数据目录data/results/vpp-limit32-20260923。

下一步优先减少重复VPP工作或合并预处理/缩略图任务，而非继续叠加全局等待；异步3FPS仍是已有短测候选但未完成长稳。未修改驱动或默认部署配置。


## 更新：同卡变速已验证链路影响

同设备2、同插槽、8→5→5→8GT/s对照完成，同步预览总INF259.78→215.33、TPU94.40%→77.82%，恢复8GT后性能恢复。独立VPP探针仅约2%下降，因此不能全归为固定VPP服务能力差异。链路速率影响已获得因果证据，具体混合负载机制尚未定位，详见 [同卡报告](same-card-link-20260923.md)。


## 2026-09-24：同卡DMA/VPP矩阵已完成

详见[DMA吞吐上限与VPP竞争报告](dma-vpp-link-20260924.md)。104档完成，527个原始文件SHA256校验一致，链路恢复8GT/s x1。本SDK路径单向最高两轮均值：PCIe2上传359.66、下载340.79MB/s；PCIe3上传689.96、下载568.33MB/s。DMA与VPP共存会改变VPP吞吐，但普通主机内存路径的吞吐平台不是物理线速饱和证明；限速组也未支持“小块调用频率为唯一主因”。报告保留所有正反轮及实际流量，仍需拆解真实业务等待与共享路径。
